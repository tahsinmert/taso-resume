# Katkıda Bulunma Rehberi

Merhaba! Bu projeye katkıda bulunmak istediğin için teşekkürler! 🎉

## Nasıl Katkıda Bulunabilirim?

### 1. Fork Yap
Önce projeyi fork yap (sağ üstteki Fork butonuna tıkla).

### 2. Clone Et
Fork'ladığın projeyi bilgisayarına indir:

```bash
git clone https://github.com/SENIN-KULLANICI-ADIN/HireLogic.git
cd HireLogic
```

### 3. Branch Oluştur
Yeni bir branch oluştur:

```bash
git checkout -b feature/yeni-ozellik-adi
```

veya bug fix için:

```bash
git checkout -b fix/hata-duzeltmesi
```

### 4. Değişikliklerini Yap
Kodunu yaz, test et, emin ol ki çalışıyor!

### 5. Commit Yap
Değişikliklerini commit'le:

```bash
git add .
git commit -m "Kısa ve açıklayıcı commit mesajı"
```

**İyi commit mesajı örnekleri:**
- `feat: yeni özellik eklendi`
- `fix: login hatası düzeltildi`
- `docs: README güncellendi`

### 6. Push Yap
Branch'ini GitHub'a gönder:

```bash
git push origin feature/yeni-ozellik-adi
```

### 7. Pull Request Oluştur
GitHub'da Pull Request oluştur ve değişikliklerini açıkla.

## Kod Standartları

- TypeScript kullanıyoruz, type'ları unutma!
- Kodunu formatla (Prettier kullanabilirsin)
- Anlamlı değişken isimleri kullan
- Gerekli yerlerde yorum ekle
- Fonksiyonları küçük tut, tek bir iş yapsın

## Sorun mu Var?

Bir sorunla karşılaşırsan Issue açabilirsin veya bana ulaşabilirsin.

## Teşekkürler! 🙏

Katkıda bulunduğun için çok teşekkürler! Her katkı değerlidir.

